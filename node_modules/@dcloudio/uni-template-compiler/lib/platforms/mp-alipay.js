const {
  capitalize
} = require('../util')

module.exports = {
  prefix: 'a:',
  formatEventType: function (eventName, isCatch) {
    return `${isCatch ? 'catch' : 'on'}${capitalize(eventName)}`
  }
}
